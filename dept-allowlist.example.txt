# One full email per line. # starts a comment; blank lines are ignored.
#
#   alice@example.com
#   bob@mails.tsinghua.edu.cn
#
# This file is loaded at startup and sha256-hashed into memory; the raw
# contents are never stored in the DB or logged. To deploy, copy this
# file to `dept-allowlist.txt` (gitignored) and add your roster.
# Registration is disabled entirely if the file is missing.

alice@example.com
bob@mails.tsinghua.edu.cn
