# One entry per line. # starts a comment; blank lines are ignored.
# Entries can be either a bare local part (the REGISTER_DOMAIN is appended)
# or a full email.
#
#   alice
#   bob@mails.tsinghua.edu.cn
#
# This file is loaded at startup and HMAC-hashed into memory; the raw
# contents are never stored in the DB or logged. To deploy, copy this file
# to `dept-allowlist.txt` (gitignored) and add your roster. The server
# warns at startup if the file is missing.

zhangsan
lisi
wangwu@mails.tsinghua.edu.cn
